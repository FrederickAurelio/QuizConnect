/** Prepared-material upload cap (must match frontend UX copy). Chunk budget limits processed segments separately. */
export const MAX_PREPARED_UPLOAD_BYTES = 8 * 1024 * 1024;

export const MAX_PREPARED_UPLOAD_LABEL = "8MB";
